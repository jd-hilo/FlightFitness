# Moved → `coaches/`

The Flight coach dashboard lives in **[`../coaches/`](../coaches/)**.

```bash
cd ../coaches
npm install
npm run dev   # http://localhost:3004
```

Do not develop against this folder; it is kept only as a redirect stub.
