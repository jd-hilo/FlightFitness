/**
 * Legacy muscle-SVG diagram — disabled.
 * RepDB provides consistent flat WebP stills via ExerciseIcon.
 */
export function ExerciseMuscleDiagram(_props: {
  catalogExerciseId?: string;
  compact?: boolean;
  maxMuscles?: number;
}) {
  return null;
}
