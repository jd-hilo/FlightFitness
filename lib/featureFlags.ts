/** Set true when full-week / day AI plan generation is ready to ship again. */
export const AI_WEEK_PLAN_ENABLED = false;

export function isAiWeekPlanEnabled(): boolean {
  return AI_WEEK_PLAN_ENABLED;
}
